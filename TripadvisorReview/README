# Requirements
Python 2

[Scrapy](https://github.com/scrapy/scrapy) (The quick way: pip install scrapy)

# How to use
`./run.sh` or `run.bat`(for windows user)(Not supported)

Get yourself a cup of coffee and wait for a few minutes, based on the
MAX_REVIEW_PAGE you set.

# KNOWN issue
The program will return nothing when too much pages crawled(trying to fix)
